import CampusLayout from "@/components/layouts/CampusLayout";

export default function CampusGroupLayout({ children }: { children: React.ReactNode }) {
  return <CampusLayout>{children}</CampusLayout>;
}